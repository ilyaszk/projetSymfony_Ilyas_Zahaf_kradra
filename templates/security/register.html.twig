<div class="card" style="width: 45%;">
    <div class="card-header">
        {#        formulaire d'insriptiont #}
        <h3 class="card-title">Formulaire d'inscription</h3>
    </div>
    <div class="card-body">
        {{ form_start(formInscr) }}
        {{ form_widget(formInscr) }}
        {{ form_end(formInscr) }}
    </div>
</div>
